import { StyleSheet, Text, View } from "react-native";

const OnboardingIqamaInput = () => {
  return (
    <View style={styles.container}>
      <Text>Onboarding Iqama Input</Text>
    </View>
  );
};

export default OnboardingIqamaInput;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
